# Porting frontier - test and gametest source sets
#
# Every path listed here is a src/test/java or src/gametest/java file that has
# not been ported to 1.21.1 yet; sourceSets.test.java and sourceSets.gametest.java
# exclude them respectively. Kept separate from porting/unported.txt so a test can
# come back the moment its subject ports, rather than all of them landing at the end.
#
# Same rules as the main ledger: it only shrinks, every listed path must exist,
# and checkPortingFrontier enforces both.
#
# Sorted, repo-relative, one path per line.
